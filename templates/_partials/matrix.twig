{% import '_includes/utils' as utils %}

{% set prevAlignment = '' %}

<div class="row row-flex">
{% for block in matrix %}
	{% include ['_matrix/' ~ block.type, '_matrix/missing'] ignore missing with { block: block } %}

	{% set prevAlignment = block['alignment'] is defined ? block.alignment : '' %}
{% endfor %}
</div>
